import { createTheme } from '@mui/material/styles';

export const newtheme = createTheme({
  palette: {
    primary: {
      main: '#0095D9',
    },
    secondary: {
      main: '#FFFFFF',
    },
  },
});