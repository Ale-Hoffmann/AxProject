import { createTheme } from '@mui/material/styles';

export const newtheme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#111111',
    },
  },
});